<% tag_list.each do |tag| %>

  <% if false %>-------
  <%= link_to tag, mogisupple_index_path(search_tag: tag), class:'btn btn-sm btn-outline-primary'%>
  -----<% end %>

  <%= link_to tag, user_path(search_tag: tag), class:'btn btn-sm btn-outline-primary'%>
<% end %>
