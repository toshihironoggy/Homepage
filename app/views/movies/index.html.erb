<h1 class="mt-3 mb-3">動画</h1>

  <% @movies.each do |movie| %>
    
    <div class="box2">
      <h2>    
      <%= link_to movie.title, "movies/#{movie.id}" %>
      </h2>
      <p>
        <% movie.tag_list.add(movie.tag_name.split(","))%>
        <%= render 'movies/tag_list', tag_list: movie.tag_list %>
      </p>
    </div>  
  <% end %>
        
        
  
    
          
    
    <%= link_to("一覧に戻る", movies_path, class:"btn btn-outline-primary btn-sm") %>
   


